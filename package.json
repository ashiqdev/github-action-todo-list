{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy:backend": "lerna exec --stream --scope=@todolist/backend -- yarn deploy",
    "build:backend": "lerna run build --stream --ignore @todolist/dashboard*",
    "start:backend": "lerna exec --stream --scope=@todolist/backend -- yarn start"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
